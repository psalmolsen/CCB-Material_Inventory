# CCBInventory — TODO

- [ ] Update 2026 aesthetics for MONTHLY DAILY OUT (daily out breakdown panel)
  - [ ] Improve contrast/readability of right-side day value badge (`.day-value-badge`, `.month-day-value`) in `src/main/resources/com/ccb/css/style.css`.
  - [ ] Improve bar/track visuals to look more like a clean “graph line” (stronger track color, clearer border/outline, more visible end cap, optional subtle grid tick effect) in `style.css`.
  - [ ] Keep existing behavior: bar length already maps to value via `fillWidth` in `MaterialMonthOutController`.
- [ ] Run project (or at least compile) to ensure no CSS typos break styling.

